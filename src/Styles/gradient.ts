import { colors } from './index'

const gradients = {
    day: [colors.primary.main, colors.primary.gradient],
    rain: [colors.primary.gradient, colors.secondary.main],
    night: ['#051A19', '#056F6D']
};

export { gradients };